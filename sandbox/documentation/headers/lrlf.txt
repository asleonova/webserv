CR (возврат каретки — carriage return)
LF (перевод строки — line feed)


CR = <US-ASCII CR, возврат каретки (13)>
LF = <US-ASCII LF, linefeed (10)>
HTTP / 1.1 определяет последовательность CR LF как маркер конца строки для всех элементов протокола, кроме объекта -тело